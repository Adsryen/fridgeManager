services:
  fridge-manager:
    build: .
    ports:
      - "8080:8080"
    environment:
      HOST: 0.0.0.0
      PORT: 8080
      SQLITE_DB_DIR: /data
    volumes:
      - fridge_data:/data

volumes:
  fridge_data:
